# 📝 Atividade Rápida

## **Primeira página HTML**

Crie um arquivo **`index.html`** com as seguintes regras:

### Regras:

- Deve conter:
    - `<!DOCTYPE html>`
    - `<html>`, `<head>` e `<body>`
    - `<title>` com o texto **"Primeira Página"**
    - Um `<h1>` com o texto **"aMinha Primeira Págin"**